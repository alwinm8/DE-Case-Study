Author : Peter Li
-----

Before test this project:

1. create a directory under maria_dev
   which is CREDIT_CARD_SYSTEM
   
   Under CREAIT_CARD_SYSTEM directory, create four subdirectories:
     
                                 CDW_SAPP_BRANCH
                                 CDW_SAPP_CREDITCARD
                                 CDW_SAPP_CUSTOMER
                                 CDW_SAPP_TIME
 
 2. After executing the query, each directory will has the corresponding table data,stored as text files.
 Each directory will has 4 text files.
                  
                   
